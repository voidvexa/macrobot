# Required: FRED API key (https://fred.stlouisfed.org/docs/api/api_key.html).
# If unset, macro/fred.py silently returns no data for all FRED-sourced series.
FRED_API_KEY=

# Optional: overrides, defaults shown below (see config.py).
# SQLITE_DB_PATH=data/macrobot.db
# TIMEZONE=America/New_York
# LOG_LEVEL=INFO
